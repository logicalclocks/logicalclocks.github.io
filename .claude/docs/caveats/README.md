# Caveats and Workarounds

Practical traps and fixes specific to this docs site.
Each caveat is a separate file — add new ones as `descriptive-name.md` with two paragraphs: first the problem, then the fix.
